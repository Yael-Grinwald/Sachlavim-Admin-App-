import { CheckValidDatePipe } from './check-valid-date.pipe';

describe('CheckValidDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckValidDatePipe();
    expect(pipe).toBeTruthy();
  });
});
